{
  "name": "yarn-workspaces-electron",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "backend",
    "shared",
    "frontend"
  ],
  "nohoist": [
    "**/electron",
    "**/electron/**",
    "**/jest",
    "**/jest/**"
  ],
  "scripts": {
    "fe": "yarn workspace frontend",
    "be": "yarn workspace backend",
    "shared": "yarn workspace @test/shared",
    "modules:watch": "yarn wsrun --stages --done-criteria=\"Watching for file\" watch",
    "modules:build": "yarn wsrun --stages build",
    "watch": "yarn --cwd electron watch",
    "build": "yarn modules:build; yarn --cwd electron dist"
  },
  "description": "Yarn workspaces with electron, for fun and profit",
  "repository": "git@github.com:aperkaz/yarn-workspaces-electron.git",
  "author": "alain <alainperkaz@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "concurrently": "^6.2.0"
  },
  "devDependencies": {
    "wsrun": "^5.2.4"
  }
}
