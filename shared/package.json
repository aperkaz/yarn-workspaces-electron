{
  "name": "@app/shared",
  "version": "1.0.0",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "scripts": {
    "watch": "yarn build -w --preserveWatchOutput",
    "build": "rimraf build; tsc --build --force"
  },
  "license": "MIT",
  "engines": {
    "node": "14.17.0"
  },
  "dependencies": {
    "sharp": "^0.28.3"
  },
  "devDependencies": {
    "@types/node": "^15.6.1",
    "rimraf": "^3.0.2",
    "typescript": "^4.3.4"
  }
}
