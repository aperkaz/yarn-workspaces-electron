{
  "name": "@test/shared",
  "version": "1.0.0",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "scripts": {
    "watch": "yarn build; tsc -w --preserveWatchOutput",
    "build": "rimraf build; tsc --build"
  },
  "license": "MIT",
  "engines": {
    "node": ">=14.15.4"
  },
  "dependencies": {
    "sharp": "^0.28.3"
  },
  "devDependencies": {
    "@types/node": "^15.6.1",
    "rimraf": "^3.0.2",
    "typescript": "^4.3.2"
  }
}
