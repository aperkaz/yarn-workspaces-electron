{
  "name": "electron",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "engines": {
    "node": ">=14.15.4"
  },
  "workspaces": {
    "nohoist": [
      "**/electron",
      "**/electron/**",
      "**/jest",
      "**/jest/**"
    ]
  },
  "scripts": {
    "postinstall": "electron-builder install-app-deps",
    "build-frontend:import": "rimraf build-frontend; mkdir build-frontend; cpx \"../frontend/build/**\" build-frontend",
    "build-backend:import": "rimraf build-backend; mkdir build-backend; cp ../backend/src/server-dev.html ./build-backend/server-dev.html; cpx \"../backend/build/**\" build-backend",
    "build-backend:watch": "rimraf build-backend; mkdir build-backend; cp ../backend/src/server-dev.html ./build-backend/server-dev.html; cpx \"../backend/build/**\" build-backend -w",
    "watch": "concurrently \"yarn build-backend:watch\" \"electron . \" ",
    "pack": "yarn build-backend:import; yarn build-backend:import; electron-builder --dir",
    "dist": "rimraf ./dist && electron-builder"
  },
  "build": {
    "appId": "Taggr",
    "mac": {
      "category": "your.app.category.type"
    }
  },
  "dependencies": {
    "concurrently": "^6.2.0",
    "electron-is-dev": "^1.1.0",
    "node-ipc": "^9.1.1",
    "sharp": "^0.28.3",
    "uuid": "^3.3.2"
  },
  "devDependencies": {
    "@types/node": "^15.6.1",
    "cpx": "^1.5.0",
    "electron": "^5.0.4",
    "electron-builder": "^22.10.5",
    "rimraf": "^3.0.2",
    "typescript": "^4.3.2"
  }
}
