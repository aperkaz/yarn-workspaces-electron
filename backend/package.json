{
  "name": "backend",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "engines": {
    "node": ">=14.15.4"
  },
  "scripts": {
    "build": "rimraf build; tsc --build",
    "start": "yarn build && node dist/index.js"
  },
  "devDependencies": {
    "@types/node": "^15.6.1",
    "rimraf": "^3.0.2",
    "typescript": "^4.3.2"
  }
}
